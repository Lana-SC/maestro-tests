appId: com.sitecapture.app.sitecapture.dev

---


# Step 3: Assert that the project name exists in API response
- assertTrue:
    expression: ${output.result != null && output.result != "" }
    timeout: 5000
    label: "Project name is fetched from API"

